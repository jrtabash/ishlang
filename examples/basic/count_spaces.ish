(defun countSpaces (str)
  (var count 0)
  (foreach c str
    (when (== c ' ')
      (+= count 1)))
  count)

(var s "Test char at op on str")
(println "Number of spaces in '" s "' is " (countSpaces s))
