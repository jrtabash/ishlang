* Add more examples

* Implement built-in data structures:
  - List
  - Array
  - Hash table

* Add string functions:
  - Length of string
  - Get char at position
  - Set char at position
  - Concatenate
  - Substring

* Rework 'break' implementation to avoid using Break::Except

* Add multi-line expression support to REPL

* Add support for comments

* Command line updates:
  - Default interpreter to interactive mode if no options are specified
  - Default interpreter to batch mode if -f option is used with no other options
